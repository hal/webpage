<% if (item.tags && item.tags.length){ %>
  <span class="visible-phone"><i class="icon-tags"></i></span>
  <% item.tags.forEach(function(tag) { %>
    <a href='<%= config.root + tag.path %>'><span class='post-tag'><%= tag.name %></span></a>
  <% }); %>
<% } %>